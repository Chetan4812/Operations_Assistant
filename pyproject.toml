[project]
name = "operations-assistant"
version = "0.1.0"
description = "A multi-agent crew on an MCP server"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "mcp>=1.0.0",
    "crewai>=0.32.0",
    "crewai-tools>=0.4.0",
    "python-dotenv>=1.0.1",
    "pydantic>=2.0",
    "pytest>=8.0",
    "litellm>=1.40.0"
]
